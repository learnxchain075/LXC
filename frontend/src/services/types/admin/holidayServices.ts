export interface IholidayForm {
  id: string;
  name: string;
  date: string;
  fromday?: string;
  toDay?: string;
  description?: string;
  schoolId: string;
  createdAt?: string;
  updatedAt?: string;
}
